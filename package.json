{
  "name": "textgame",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "main.js",
  "scripts": {
    "dev": "cross-env NODE_ENV=development concurrently -k \"vite\" \"wait-on http://127.0.0.1:5173 && electron .\"",
    "build": "vite build",
    "preview": "vite preview",
    "start": "cross-env NODE_ENV=production electron ."
  },
  "devDependencies": {
    "cross-env": "^7.0.3",
    "concurrently": "^9.1.2",
    "electron": "^34.0.0",
    "vite": "^6.0.0",
    "wait-on": "^8.0.1"
  },
  "dependencies": {
    "@chenglou/pretext": "^0.0.4"
  }
}
